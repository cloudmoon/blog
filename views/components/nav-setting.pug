.nav-setting
  .ui.buttons
    .ui.floating.dropdown.button
      i.icon.bars
      .menu
        if user
          a.item(href=`/posts?author=${user._id}`) 个人主页
          .divider
          a.item(href='/posts/create') 发表文章
          a.item(href='/logout') 登出
        else
          a.item(href='/login') 登录
          a.item(href='/signup') 注册
